#include "main.h"

/**
 * _strlen - returns the length of a string
 * @s: string input to evaluate
 * Return: the length of the string
 */

int _strlen(char *s)
{
	int n = 0;

	while (s[n] != '\0')
	{
		i++;
	}
	return (n);
}
