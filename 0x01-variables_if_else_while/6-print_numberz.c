#include <stdio.h>
/**
 * main - a program that prints
 * all single digit number of base
 * ten starting from zero
 * Return: 0
 */

int main(void)
{
	int y = 0;

	for (y = 0; y < 10; y++)
{
	putchar(y);
}

	putchar('\n');
	return 0;
}
