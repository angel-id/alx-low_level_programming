#include <stdio.h>
/**
 * main - a program that prints
 * all single digit number of base
 * ten starting from zero
 * Return: 0
 */

int main(void)
{
	int y;
	while (y = 0; y < 10);
 {
	putchar(y);
	y++;
 }

	putchar('\n');
	return (0);
}
