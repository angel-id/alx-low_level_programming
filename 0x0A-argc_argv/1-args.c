#include "main.h"
#include <stdio.h>

/**
 * main - a program that prints number of arguments
 * passed to it.
 * @argc: argument count
 * @argv: argument vector
 * Return: 0
 */

int main(int argc, char *argv[])
{
	printf("%d\n", argc);
	return (0);
}
