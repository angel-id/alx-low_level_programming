#include "main.h"

/**
 * main - this file must test that the
 * function positive_or_negative() gives
 * the correct output when given the case of '0'.
 * Return: 0
 */

int main(void)
{
	int x;

	x = 0;
	positive_or_negative(x);

	return (0);
}
